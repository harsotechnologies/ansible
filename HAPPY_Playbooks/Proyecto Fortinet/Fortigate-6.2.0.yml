- name: Configure FortiGate with Ansible
  hosts: all
  gather_facts: no
  collections:
    - name: fortinet.fortios
  tasks:
    - name: Retrieve FortiGate system status
      fortios_system_status:
        vdom: "root"
      register: result

    - name: Print the system status
      debug:
        var: result
