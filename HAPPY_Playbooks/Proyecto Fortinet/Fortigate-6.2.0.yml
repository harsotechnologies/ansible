- name: Configure FortiGate with Ansible
  hosts: all
  gather_facts: no
  collections:
    - fortinet.fortios
  tasks:
    - name: Retrieve FortiGate system status
      fortios_aplication_name:
      register: result

    - name: Print the system status
      debug:
        var: result
