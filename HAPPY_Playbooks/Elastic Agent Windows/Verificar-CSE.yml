---
- name: Descargar y ejecutar script de PowerShell
  hosts: all

  vars: 
     os_type:  ["RedHat", "Debian", "Windows"]


  tasks:
    - name: create groups based on distro
      group_by: key={{ ansible_distribution }}

    - name: Distribucion  del sistema
      hosts: Windows
      debug:
        msg: "Es sistema operativo que  está instalado es {{ansible_distribution}}"
