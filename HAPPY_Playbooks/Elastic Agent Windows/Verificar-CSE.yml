---
- name: Descargar y ejecutar script de PowerShell
  hosts: all
  gather_facts: yes
  vars: 
     os_type:  ["RedHat", "Debian", "Windows"]

  tasks:
       - name: create groups based on distro
         group_by: key= {{ ansible_facts['os_family'] }}

    
- name: Distribuciones basadas en windows
  hosts: Windows
  tasks:
   - name: Mostras sistema
     debug:
       msg: "Es sitama operativo que  está instalado es {{ ansible_distribution }}"
